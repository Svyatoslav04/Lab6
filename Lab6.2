#include <stdio.h>

typedef struct {
    int (*add)(int, int);
    int (*subtract)(int, int);
    int (*multiply)(int, int);
} MathOperations;

int add(int a, int b) {
    return a + b;
}

int subtract(int a, int b) {
    return a - b;
}

int multiply(int a, int b) {
    return a * b;
}

int main() {
}
